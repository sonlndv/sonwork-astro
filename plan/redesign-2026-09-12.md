# sonwork.org redesign — 2026-09-12

Status: draft. Not accepted. Not implementation.

OMH record (scaffold only): `.omh/plans/2026-09-12T110342543086Z-redesign-sonwork-org-home-about-portfolio-new-story-new-look-fre-a0717c.md`

## Clarified brief

**Outcome.** Home, about, and project profiles tell the current work (Lever is the core; Sonar researches; Beacon makes it found; this site is the shopfront) and those surfaces get a new composition. Not a colour-token pass.

**Constraints.**
- Frozen: LS15, Son's blue (`--lume`), `/readings/` archive look and URLs.
- Hands off `web.sonwork.org` and `~/arx-web`. No Arx tokens, components, or copy.
- No employer / client names. No WeCare. Humble voice. Short, raw, bullets.
- Booking stays hidden until mail is wired (`771ca43`).
- Never push `main`. Jarvis reviews. Son publishes.

**Assumption (success criteria were not asked).** A stranger can say who Sơn is, what he is building, and find the archive as secondary. Both themes and a phone width look intentional. `npm run test`, `check`, `build` pass. DESIGN.md matches what shipped.

## Repo facts

- Repo: `~/sonwork-astro` → https://sonwork.org
- Product/design law: `PRODUCT.md`, `DESIGN.md`
- Prior plan already exists: `plan/personal-site-revamp.md` (profile-led home, Book time as primary CTA)
- Colour-system work is **paused** (`plan/colour-system-state.md`): Son: "it really isnt help". Do not resume.
- Mid-flight UI already in git: geometry, colour hierarchy, Pulse, Depth. DESIGN.md still forbids cards, glass, background pattern.
- `PRODUCT.md` still says "No real reports exist yet" — stale vs the live archive.

## Options

| | Chosen | Rejected |
|---|---|---|
| A | New story + new **composition** on `/`, `/about/`, `/projects/*`. Keep `--lume` and LS15. Update DESIGN.md for structure/copy. Leave `/readings/` alone. | |
| B | Resume the paused colour-token pass | Son already stopped it |
| C | Copy `web.sonwork.org` / Arx look onto sonwork | Brand wall; user froze that site |
| D | Make Book time the home headline now | Mail not wired; not asked this session |

**Pick: A.** Use `plan/personal-site-revamp.md` for IA hints (profile-led home, readings secondary) but do not import Arx grammar or unhide booking.

## Work, if accepted

1. Rewrite home copy and section order to the Lever / Sonar / Beacon / shopfront story. Keep the day's-run readings block as a bridge, not the identity.
2. Restyle those pages: type scale, spacing, section rhythm. Do not invent a new hue.
3. Same pass on `/about/` and project profiles (largest open gap per colour-system-state).
4. Update `DESIGN.md` and `PRODUCT.md` in the same change. Diary entry for the day.
5. Branch diff. Screenshots: dark, light, 375px. No `main`.

## Risks

- DESIGN.md refusals vs Depth/Pulse already live — resolve in DESIGN.md, do not sneak ornament back.
- Copying Arx "structure" is how brand-wall leaks start. Don't.
- Unhiding `/book/` without mail is a product lie.
- Colour pass already failed Son's eye. Composition, not chroma.

## Acceptance

- [ ] `/`, `/about/`, `/projects/*` state the current portfolio in PRODUCT.md language
- [ ] LS15 unchanged; `--lume` still the only machine accent; `--human` only for Sơn
- [ ] `/readings/` routes, Field map, and archive chrome unchanged
- [ ] `web.sonwork.org` / `~/arx-web` untouched
- [ ] DESIGN.md + PRODUCT.md true after the change
- [ ] `npm run test` · `npm run check` · `npm run build` pass
- [ ] Screenshots: both themes, 375 / 768 / 1280, from built CSS
- [ ] Branch only. `Not approved` until Jarvis + Son

## Verification (when implementing)

```
git diff --stat -- ~/arx-web   # must be empty
npm run test
npm run check
npm run build
```

Then screenshot `/`, `/about/`, one project profile, `/readings/` (prove archive unchanged).

## Handoff after acceptance

**Not started.** Direct webbuilder on a branch — one owner, this repo, visual + copy. Not Codex. Not ultrawork. Not loop.

Say "accept, build it" to start. Anything else stays draft.
