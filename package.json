{
  "name": "sonwork",
  "type": "module",
  "version": "0.1.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist --force-language en",
    "preview": "astro preview",
    "check": "astro check",
    "dev:worker": "wrangler dev --var DEV_EMAIL:son@perfeat.org",
    "deploy": "npm run build && wrangler deploy",
    "sync": "node scripts/sync-gbrain.mjs"
  },
  "dependencies": {
    "astro": "^6.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "pagefind": "^1.1.1",
    "typescript": "^5.6.3",
    "wrangler": "^4"
  }
}
